# 2666. Allow One Function Call
#Medium  

```js
var once = function(fn) {

    let result ;
    let useFN = false;
    return function(...args){
        if(!useFN){
            resulte = fn(...args)
            useFN = true;
            return resulte;
        }
        return undefined;
    }
};
```

в чем прикол этой штуки 

нам надо чтобы она запоминала вызывалась она или нет вот поэтому тут есть переменая которая следит за этим она называеться useFN что расшифровываоться как использывание функции и она запоминает как бала ли она вызвана 

а потом просто в переменую записываеться все дынные из массива при помоши оператора... spred и возвращяет эту переменую

прикольнльная задачка 
