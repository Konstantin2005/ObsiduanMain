# 2724. Sort By 
#Medium 

```js
/**
 * @param {Array} arr
 * @param {Function} fn
 * @return {Array}
 */
var sortBy = function(arr, fn) {
    return arr.sort((a,b) =>fn(a) - fn(b))
};
```

суть просто отсорторивать масси в функии FN
то есть в целом карна буедт выглелить так
```js 
const arr = [1,223,324,1];
const Sort = function(a,b){
return sort(a,b) //какаято сортировка но это не суть

}

sortBy(arr,Sort)
```

примерно так но не так точно но это не суть 