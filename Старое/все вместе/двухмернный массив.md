#структуры_данных 

`int` `numbers[3][2]`

`{`

    `{1, 2},`     `// первая строка`

    `{4, 5},`     `// вторая строка`

    `{7, 8}`      `// третья строка`

`};` 

двухмернный массив это соединение 2 массивов в один .а точнее в один массив помещяються другие массивы , но это не суть 

`int` `numbers[3][2] {};так достаем элементы и засоувывоем их