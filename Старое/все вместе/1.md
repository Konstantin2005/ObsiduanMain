[1. Two Sum](https://leetcode.com/problems/two-sum/)

#Easy
задача с каких двух элементов можно составить target 

```js
var twoSum = function(nums, target) {

    let numToIndexMap = {};

  

    for (let i = 0; i < nums.length; i++) {

        let diff = target - nums[i];

        if (numToIndexMap.hasOwnProperty(diff)) {
            return [i, numToIndexMap[diff]];
        }
        numToIndexMap[nums[i]] = i;
    }
    // If no two numbers add up to the target, return null
    return null;
};
```

2. метод
грубой силы просто сложить все 
```js
var twoSum = function(nums, target) {  
    for (let i = 1; i < nums.length; i++) {  
        for (let j = i; j < nums.length; j++) {  
            if (nums[j] + nums[j - i] === target) {  
                return [j - i, j];  
            }  
        }  
    }  
    return []; // Empty array if no solution found  
};
```

вложенный цикл начинает он с одного потому-что хитрый метод

это сделано что-бы не выйти за пределы массива 
