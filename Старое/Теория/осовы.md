#СУБД 
````sql
 1) Select * /*это вызов всей таблицы*/ 

2)Select category_id  /* это вызов столбца category_id из таблицы castomers*/
 from castomers  

3)select distinct /*city это команда удаляет все дубликаты из столбца в таблице castomers*/
   from castomers  
 
 4)select count country /*эта команда возвращяет количество строк в столбце в ТАб Cast*/

5)select company_name, contact_name, phone
   from customers
   where country = 'USA' /*- эта команда возвращяет все стороки с названием USA*/


6) select * 
  from products
  where unit_prise > 25 and units_in_stock > 40;
