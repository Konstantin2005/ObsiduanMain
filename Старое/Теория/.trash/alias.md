#Git 
эта штука позволяет делать свои стеки команд на подобии открой то или то

git config --global alias.c config 'config --global'

мы сделали команду с которая будет вызывать config и так можно с чем угодно

можно ещё создать стек команд 

git config --global alias.sayhi '!esho "hello";esho"from git"'
