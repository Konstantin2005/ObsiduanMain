```Go
select { 

case x := <-ch1: // блок выполнится, если раньше всего прочитается значение из 
case y := <-ch2: // блок выполнится, если раньше всего прочитается значение из канала 
case ch3 <- z: // блок выполнится, если раньше отправится значение в канал 

default: // опциональный блок // выполняется когда не сработало ни одно из условий }
```

Выбор бля проверки происходит в случайном порядке
Из case соотвественно если они все проверены то проверяеться default



#One 
