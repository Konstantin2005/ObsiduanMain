#typeSctipt
```ts
let list :number[] = [1,2,3]; // база  
  
let list2:Array<number> = [1,3,5]; //из С++ создания класса "Array"это слово которое дает тип данных массиву  
  
let x:[number,string]; //масии из разных типов данных , точнее одного элемента  
  
x = [1,"get"];  
  
let e:[number,string]= [1,"string"] //тоже самое но в 1 строку  
  
  
enum directions{  
    UP= 32,  
    DOWN = 33,  
    LEFT = 35,  
    RIGHT = 36,  
}  
  
console.log(directions.DOWN);

directions[2]
```

массивы и как их создовать и применять 