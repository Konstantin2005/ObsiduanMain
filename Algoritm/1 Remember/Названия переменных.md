#Two 
Вот есть функция
```java
public List getThem() { 
List list1 = new ArrayList(); 
for (int[] x : theList) 
  if (x[0] == 4) list1.add(x); 
  return list1; 
  }
```


1 . Какие данные хранятся в theList? 
Я не знаю, вообще. Какой-то массив

2 . Чем так важен элемент theList с нулевым индексом? 
Я не знаю. Может он большой или маленький

3 . Какой особый смысл имеет значение 4? 
Тоже не знаю. код не понятный вообще

4 . Как будет использоваться возвращаемый список?
В душе не знаю. Просто код какой-то и все


```java
public List getFlaggedCells() { 
List flaggedCells = new ArrayList(); 
for (int[] cell : gameBoard) 
if (cell[STATUS_VALUE] == FLAGGED) 
	flaggedCells.add(cell); 
return flaggedCells; 
}
```

1 . Какие данные хранятся в theList? 
Это игровая доска

2 . Чем так важен элемент theList с нулевым индексом? 
Если там 0 4 индекс, то флажек установлен.

3 . Какой особый смысл имеет значение 4? 
Это означает. что в этом массиве с этим индексом флажек установлен

4 . Как будет использоваться возвращаемый список?
Будет понятно где установлены флажки и как-то это будет использоваться в программе