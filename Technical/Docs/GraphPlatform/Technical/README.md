# Graph Platform Technical Archive

This folder contains versioned technical plans and architecture notes for the Graph Platform workstream.

## Folder Rule

- Keep versioned plans here: `v*.md`.
- Keep project entrypoints one level up in `Technical/Docs/GraphPlatform`.
- Keep runtime scripts in `Scripts` and tests in `Tests` so existing automation paths stay stable.

## Current Entrypoints

- `../SKill` - active project protocol and execution mode.
- `../execution-log.md` - chronological implementation log.
- `../bugs.md` - bug and follow-up registry.

