
Алиасы , эта штука когда есть одинаковые по имени пакетов , и надо чтобы был выполнин , опеределенный 
```Go
import (
	 "math/rand"
 rnd "crypto/rand" )
```

```GO
rng
```


что-бы не грамоздить код есть шутка которая убирает , названия файла из которого , береться метод 

```Go
package main 
import . "fmt" 
func main() { 
// не указываем префикс 
fmt Println("Используем импорт с точкой") }
```

это стоит использовать с осторожностью 

Импорт с точкой следует использовать только тогда, когда это действительно оправдано. Лучше всё же применять обычный импорт и явно указывать имя пакета перед идентификаторами.