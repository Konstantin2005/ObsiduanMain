#String
Убрать все пробелы " " было Стало ""
```Go
val ,err := Strings.ReplaceALL(str , old " ",new "") 
```

Перевод из string в int и наоборот 

```Go 
val, _ := strconv.Atoi(string(num)) // переводит string в int  
s := strconv.Itoa(val)              // переводит int в string
```


Перевод из string в Массив string c сепаратором " "

```Go
array := strings.Split(s, " ")      // переводит sting в массив string
```