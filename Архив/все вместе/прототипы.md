#javaSctipt
```js

//прототипы
const person =new Object({  
    name:'Maxim',  
    age:'12',  
    greet: function(){  
        console.log('привет')  
    }})  
  
Object.prototype.SayHello = function(){  
    console.log('name')  
}  
  
const lena = Object.create(person) ({  
    name:'LENA'  
})
```

прототипы это что-то на подобии  наследованости 
которая основана на обьектах 

есть один главный обьект от которого наследуеться все остальные это называеться object он включает в себя много стандартных методов 

и после мы уже навешываем на этот начальный еще большей своих методов

то есть прототип это нынешния форма обьекта которая была много раз наседована 