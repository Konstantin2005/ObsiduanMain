## 2695. Array Wrappe 
#Medium 
```js

var ArrayWrapper = function(nums) {
    this.array = nums
};

ArrayWrapper.prototype.valueOf = function() {
    return this.array.reduce((pre ,cur) => pre+cur ,0);
}
ayWrapper.prototype.toString = function() {
    return JSON.stringify(this.array)
}
```

я так и не понял что она у меня так рещена

задача основана на сласах и прототипах 

есть класс ArrayWrapper у него есть 2 метода 
1 , просто складывает все числа массива и возвращяет массив с 1 часлом
2.менят все числа массива в string и возвараешет массив с стирингом